from setuptools import setup

setup(name='gym_poker',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'prettytable']
      )

setup(name='gym_poker',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'prettytable', 'tensorflow', 'keras==2.1.6']
      )
